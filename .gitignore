# Ordner/Dateien, die NICHT ins Repo sollen
node_modules/
dist/
dist-web/
dist-manual/
*.log
.vscode/
.DS_Store
.github/
.husky/
# Build-/Release-Artefakte
dist/
dist-electron/
dist-release/
build/
*.log
*.zip
# Code-Signing Zertifikate (NIEMALS committen!)
*.pfx
*.p12
*.cer

# 🚨 DATABASE PROTECTION - NIEMALS Kundendaten committen! (Updated 2025-11-06)
# MANDATORY: Prevent all database files from being committed
db/*.db
db/*.db-*
db/**/*.db
db/**/*.db-*
db/archive-migration-backups/*.db
db/rawalite-dev-copy.db
db/rawalite-dev-copy_*.db

# CRITICAL: Production database should NEVER be in repo
# Real location: C:\Users\ramon\AppData\Roaming\Electron\database\rawalite.db
# Dev location: C:\Users\ramon\AppData\Roaming\Electron\database\rawalite-dev.db
# Repository should only contain PLACEHOLDER files

# Ensure only .PLACEHOLDER files are allowed in db/
!db/*.PLACEHOLDER
!db/**/*.PLACEHOLDER

# Auto-generated build metadata (esbuild)
meta.json

# 📌 BACKUP FILES (FILE REPLACEMENT POLICY - 29.10.2025)
# MANDATORY: All backup files from file replacement operations
*.backup
*.backup-*

# 📁 DOCUMENTATION FOLDERS (NOT in repo - local only)
# /docs is for LOCAL project documentation only
# Documentation should NOT be committed to version control
# PLACEHOLDER-only pattern below
docs/
!docs/*.PLACEHOLDER
!docs/**/*.PLACEHOLDER

# 🔐 ENVIRONMENT-SPECIFIC CONFIGS (Prevent accidental commits)
.env
.env.local
.env.*.local
src/config/*.local.*
electron/config/*.local.*

# 🔐 LOCAL IDE CONFIGURATIONS WITH POTENTIAL SECRETS
.vscode/settings.local.json
.vscode/launch.local.json

# 🔐 SENSITIVE DOCUMENTATION PATTERNS (Future prevention)
# Prevent accidental commit of sensitive documentation if added in future
docs/**/*secret*
docs/**/*password*
docs/**/*apikey*
docs/**/*credential*