# Normalisiere Zeilenenden auf LF im Repo, Windows arbeitet lokal mit CRLF weiter
* text=auto eol=lf

# Binary (niemals normalisieren)
*.png binary
*.ico binary
*.pdf binary
*.wasm binary

# Lockfiles / YAML sollten LF bleiben
*.yml text eol=lf
*.yaml text eol=lf
pnpm-lock.yaml text eol=lf
