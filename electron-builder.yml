appId: com.rawalite.app
productName: RawaLite

files:
  - dist/**
  - dist-electron/**
  - package.json

extraResources:
  - from: templates
    to: app/templates

win:
  target: nsis
  # publisherName:  # ← ENTFERNT / NICHT setzen!

publish:
  - provider: github
    owner: MonaFP
    repo: RawaLite
    releaseType: release
